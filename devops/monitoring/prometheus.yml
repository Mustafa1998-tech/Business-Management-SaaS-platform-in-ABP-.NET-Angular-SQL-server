global:
  scrape_interval: 15s

scrape_configs:
  - job_name: saas-api
    metrics_path: /metrics
    static_configs:
      - targets: ['api:8080']

  - job_name: kubernetes-pods
    kubernetes_sd_configs:
      - role: pod
